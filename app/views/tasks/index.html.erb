<h1 class="display-3">My Tasks</h1>
<ul>
  <% @tasks.each do |task|%>
  <li><%= link_to(task.title, task_path(task.id), class: "text-decoration-none") %></li>
  <% end%>
</ul>

<%= link_to 'Add Task', tasks_new_path, class: "text-decoration-none" %>
